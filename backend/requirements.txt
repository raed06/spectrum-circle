# Core Framework & Utilities
# Fundamental settings, environment variables, and core data structures.
python-dotenv==1.2.1
pydantic==2.12.4
typing_extensions==4.15.0  # For Pydantic and type hints
annotated-types==0.7.0
loguru==0.7.3             # For structured logging
httpx==0.28.1             # General purpose HTTP client

# API & Web Services
# Web framework, server, and related networking tools.
fastapi==0.121.2
uvicorn==0.38.0
starlette==0.49.3
websockets==15.0.1
python-multipart==0.0.20  # For handling form data
httptools==0.7.1          # For ASGI server

# Database & ORM
# Database connectors and Object-Relational Mapper (ORM).
SQLAlchemy==2.0.44        # Primary ORM
alembic==1.17.2           # Database migrations for SQLAlchemy
psycopg2-binary==2.9.11   # PostgreSQL connector
redis==7.0.1              # Redis client

# AI/LLMs & Fine-Tuning (Hugging Face)
# Core libraries for loading models, LoRA, and training on datasets.
torch==2.9.1              # Core PyTorch library
transformers==4.57.1      # Model loading and tokenization
accelerate==1.12.0        # Distributed training/GPU management
peft==0.18.0              # Parameter-Efficient Fine-Tuning (LoRA)
bitsandbytes==0.48.2      # 8-bit quantization for efficient memory
safetensors==0.6.2        # Safer serialization format
tokenizers==0.22.1

# AI/LLMs (Google Generative AI)
# Google's specific SDKs for AI services.
google-generativeai==0.8.5
google-cloud-aiplatform==1.128.0 # For Vertex AI services

# Vector Store & Embedding
# Tools for handling vector databases and generating embeddings.
chromadb==1.3.5
sentence-transformers==5.1.2 # For local embedding generation

# MLOps, Tracing, & Experiment Tracking
# Tools for monitoring, logging experiments, and tracing LLM calls.
mlflow==3.6.0
langsmith==0.4.43
langfuse==3.10.1
opentelemetry-sdk==1.38.0 # Core for tracing/observability

# Data Processing & Analysis
# Standard libraries for data manipulation and scientific computing.
pandas==2.3.3
numpy==2.3.5
scipy==1.16.3
scikit-learn==1.7.2
matplotlib==3.10.7

# Testing
# Frameworks for unit and integration testing.
pytest==9.0.1
pytest-asyncio==1.3.0

# Security & Authentication
# Handling security concerns, encryption, and authorization.
cryptography==46.0.3
python-jose==3.5.0      # JSON Web Encryption (JWE) and related standards
Authlib==1.6.5